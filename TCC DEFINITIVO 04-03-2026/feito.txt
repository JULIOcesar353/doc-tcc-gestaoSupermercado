Quero que você ajuste SOMENTE a paleta de cores do meu projeto (React + CSS), sem alterar estrutura, layout, espaçamento ou comportamento dos componentes.

Objetivo:
Melhorar contraste e hierarquia visual usando uma paleta consistente, mantendo o layout atual.

Use exatamente essas variáveis:

:root {
  --bg-main: #f1f5f9;
  --bg-card: #ffffff;
  --border: #e2e8f0;

  --text-primary: #1e293b;
  --text-secondary: #64748b;

  --primary: #2563eb;
  --primary-hover: #1d4ed8;
  --primary-light: #dbeafe;

  --sidebar-bg: #0f172a;
  --sidebar-hover: #1e293b;
  --sidebar-active: #2563eb;
  --sidebar-text: #e2e8f0;

  --success: #22c55e;
  --success-bg: #dcfce7;

  --warning: #f59e0b;
  --warning-bg: #fef3c7;

  --danger: #ef4444;
  --danger-bg: #fee2e2;

  --info: #3b82f6;
  --info-bg: #dbeafe;
}

Regras obrigatórias:

NÃO alterar:

estrutura HTML/JSX
espaçamentos (margin/padding)
tamanhos
posicionamento
lógica dos componentes
Substituir apenas cores hardcoded por variáveis

Aplicar hierarquia correta de cores:

fundo geral → --bg-main
cards/modais → --bg-card
bordas → --border
textos principais → --text-primary
textos secundários → --text-secondary
Elementos interativos:

botões principais → --primary
hover → --primary-hover
estados leves → --primary-light
Sidebar:

fundo → --sidebar-bg
item hover → --sidebar-hover
item ativo → --sidebar-active
Status (não mudar estrutura, só cores):

sucesso → verde
aviso → amarelo
erro → vermelho
info → azul
Garantir CONTRASTE:

nunca usar cores muito próximas entre fundo e conteúdo
textos devem ser sempre legíveis
elementos importantes devem se destacar do fundo
Importante:
Não redesenhar componentes. Apenas melhorar a aplicação de cores para criar hierarquia visual clara e consistente.